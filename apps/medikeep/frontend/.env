# Vite Environment Variables
# API URL for the FastAPI backend
# Use relative URL in development so Vite proxy works (vite.config.ts)
# Vite dev server proxies /api/* requests to http://localhost:8000
VITE_API_URL=/api/v1

# App Name
VITE_NAME=MediKeep

# Debug mode
VITE_DEBUG=true
